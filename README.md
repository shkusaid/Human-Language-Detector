# Human-Language-Detector
A Python project that detects the language of a given text using a Naive Bayes classifier and translates it into a selected language using pretrained transformer models. Supports multiple languages based on your dataset.
